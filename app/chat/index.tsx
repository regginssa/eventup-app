import { ChatContainer } from "@/components";
import { View } from "react-native";

const ChatScreen = () => {
  return (
    <ChatContainer>
      <View></View>
    </ChatContainer>
  );
};

export default ChatScreen;
